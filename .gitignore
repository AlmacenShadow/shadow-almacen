*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db

# DB local de desarrollo (en producción usamos MySQL del cPanel)
/database/database.sqlite

# Archivos temporales de nuestra sesión de pruebas
serve-out.log
serve-err.log
php-out.log
php-err.log

# IMPORTANTE: vendor/ NO se ignora porque el hosting cPanel no tiene SSH
# para correr `composer install`. Se sube al repo y se actualiza cuando
# cambien las dependencias. ~50MB extra pero deploy funciona sin shell.
