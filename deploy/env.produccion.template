# ============================================================
# Shadow Almacén — .env de PRODUCCIÓN
# ============================================================
# Copiar este archivo al servidor cPanel como ".env" en la
# carpeta public_html/almacen.shadowpanama.com/
# Reemplazar los <<XXX>> con los datos reales de la base de datos.
# ============================================================

APP_NAME="Shadow Almacén"
APP_ENV=production
APP_KEY=base64:uudwQL2dXgZoLoGIvVGGSmYweSevQ0sRJ/vz/UV+3bU=
APP_DEBUG=false
APP_URL=https://almacen.shadowpanama.com

APP_LOCALE=es
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=es_ES

APP_MAINTENANCE_DRIVER=file
BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=warning

# --- Base de datos MySQL ---
DB_CONNECTION=mysql
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=<<NOMBRE_DB>>
DB_USERNAME=<<USUARIO_DB>>
DB_PASSWORD=<<PASSWORD_DB>>

# --- Sesión / Cache ---
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

CACHE_STORE=database
QUEUE_CONNECTION=database

# --- Email (configuración para enviar notificaciones a futuro) ---
MAIL_MAILER=smtp
MAIL_HOST=mail.shadowpanama.com
MAIL_PORT=465
MAIL_USERNAME=almacen@shadowpanama.com
MAIL_PASSWORD=lpdpppcthctf2026
MAIL_ENCRYPTION=ssl
MAIL_FROM_ADDRESS="almacen@shadowpanama.com"
MAIL_FROM_NAME="${APP_NAME}"

# --- Filesystem ---
FILESYSTEM_DISK=local
